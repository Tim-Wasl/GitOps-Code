
pipeline {
  agent any

  tools {
    jdk '17 (OpenJDK)'
  }

  stages {
    stage('build') {
      steps {
       
          sh "./gradlew test jacocoTestReport"
          sh "./gradlew build " 
      }
    }
  }
}
